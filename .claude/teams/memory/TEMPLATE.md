# Team lessons — <team>
> Appended by team-run report stages (one `## <timestamp> <ticket>` section per run with lessons).
> Pruned/consolidated by the librarian. Durable conventions graduate up into the context pack.

<!--
  This file is the team's growing memory. The runner's Report stage appends a
  dated section here whenever a run surfaces durable lessons (the `lessons` field
  in each stage's structured report). Over many runs it becomes the team's hard-won
  operational knowledge.

  Lifecycle:
  - APPEND: runs add `## <ISO-timestamp> <TICKET-ID>` blocks with `- (<stage>) <lesson>` lines.
  - PRUNE: a periodic librarian pass consolidates duplicates and drops stale notes.
  - GRADUATE: a lesson that proves durable and broadly relevant moves into the
    context pack's Trip-wires section (memory is the holding area; the pack is canon).

  Keep this seed heading (`# Team lessons — <team>`) — the memory-seed test asserts it.
  Below is the shape the runner writes; leave one example or delete it once real runs land.
-->

## <ISO-timestamp> <TICKET-ID>
- (decompose) <a planning-stage insight a future run should not rediscover>
- (implement:1) <an implementation gotcha specific to this team's zones>
- (test) <a testing/fixture nuance — e.g. a shared resource that needs reset between tests>
- (review#1) <a review-stage pattern worth requiring on similar changes>
