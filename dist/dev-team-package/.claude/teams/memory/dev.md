# Team lessons — dev
> Appended by team-run report stages (one `## <timestamp> <ticket>` section per run with lessons).
> Prune/consolidate periodically. Durable conventions graduate up into the context pack.

<!-- Empty seed. The runner appends dated `## <timestamp> <TICKET>` blocks with
     `- (<stage>) <lesson>` lines as runs surface durable lessons. Leave this heading. -->
